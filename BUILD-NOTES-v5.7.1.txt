Reading MMO v5.7.1 — Targeted Fix Build

Base: v5.7 Full Height Home

Actual fixes in this build:
1. App Health reports v5.7.1 instead of the stale hard-coded v3.5.
2. Home no longer stretches the short v5.7 footer or leaves a giant empty brown zone. A purpose-built tall bookcase extension is assembled from the existing pixel-art shelves/frame at native scale, with a second mirrored shelf tier filling the lower screen.
3. Reading Home tab spacing is repaired so session/chapter and Last Finished fields have separate rows and stay inside the illustrated pages.
4. Me/Profile tabs use a five-column grid so Stats, Setup, Journal, Titles, and Save all fit onscreen without horizontal clipping.
5. Service-worker cache key is bumped to v5.7.1. Update/reload now waits for worker activation/controller change and then performs a cache-busted navigation.

No game progression, save data, quest logic, loot logic, or KWGT data schema was changed.
